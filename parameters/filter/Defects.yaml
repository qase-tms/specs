name: filters
in: query
style: form
explode: true
schema:
  type: object
  properties:
    status:
      type: string
      enum:
        - open
        - resolved
        - in_progress
        - invalid
