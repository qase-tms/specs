description: A Custom Field.
content:
  application/json:
    schema:
      allOf:
        - $ref: './Response.yaml#/content/any/schema'
        - type: object
          properties:
            result:
              $ref: '../schemas/CustomField.yaml'
