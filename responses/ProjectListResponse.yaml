description: A list of all projects.
content:
  application/json:
    schema:
      allOf:
        - $ref: './Response.yaml#/content/any/schema'
        - type: object
          properties:
            result:
              type: object
              properties:
                total:
                  type: integer
                filtered:
                  type: integer
                count:
                  type: integer
                entities:
                  $ref: '../schemas/Projects.yaml'
