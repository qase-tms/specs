description: A list of found entities.
content:
  application/json:
    schema:
      allOf:
        - $ref: './Response.yaml#/content/any/schema'
        - type: object
          properties:
            result:
              type: object
              properties:
                entities:
                  type: array
                  items:
                    oneOf:
                      - $ref: '../schemas/TestCase.yaml'
