type: object
properties:
  size:
    type: integer
  mime:
    type: string
  filename:
    type: string
  url:
    type: string
    format: uri


              'hash' => $attachment->hash,
              'size' => $attachment->size,
              'mime' => $attachment->mime,
              'filename' => $attachment->filename,
              'url' => get_attachment_url($attachment, $team->hash)
