type: object
properties:
  title:
    type: string
    maxLength: 255
  action:
    type: string
    deprecated: true
    description: Deprecated, use the `steps` property instead.
  expected_result:
    type: string
    deprecated: true
    description: Deprecated, use the `steps` property instead.
  data:
    type: string
    deprecated: true
    description: Deprecated, use the `steps` property instead.
  steps:
   type: array
   items:
     $ref: 'SharedStepContent.create.yaml'
required: [title]
