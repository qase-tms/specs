type: object
properties:
  status:
    type: integer
  position:
    type: integer
    deprecated: true
  attachments:
    type: array
    items:
      $ref: 'Attachment.yaml'
  steps:
    type: array
    description: 'Nested steps results will be here. The same structure is used for them for them.'
    items:
      type: object
