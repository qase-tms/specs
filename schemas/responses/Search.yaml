allOf:
  - $ref: './Base.yaml'
  - type: object
    properties:
      result:
        type: object
        properties:
          entities:
            type: array
            items:
              oneOf:
                - $ref: '../TestCase.yaml'
