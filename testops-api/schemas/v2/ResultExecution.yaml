type: object
properties:
  start_time:
    type: number
    format: double
    nullable: true
  end_time:
    type: number
    format: double
    nullable: true
  status:
    type: string
    description: Can have the following values passed, failed, blocked, skipped, invalid + custom statuses
  duration:
    type: integer
    format: int64
    nullable: true
  stacktrace:
    type: string
  thread:
    type: string
required:
  - status
