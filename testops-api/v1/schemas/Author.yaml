type: object
properties:
  id:
    type: integer
    format: int64
  uuid:
    type: string
    format: uuid
    description: Author UUID. Use it to reference the author in other API methods.
  author_id:
    type: integer
    format: int64
    deprecated: true
    description: Deprecated, use `uuid` instead.
  entity_type:
    type: string
  entity_id:
    type: integer
    format: int64
  email:
    type: string
  name:
    type: string
  is_active:
    type: boolean
