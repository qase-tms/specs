type: object
properties:
  data:
    type: string
  hash:
    type: string
    deprecated: true
  action:
    type: string
  expected_result:
    type: string
  attachments:
    type: array
    items:
      $ref: 'Attachment.hash.yaml'
  steps:
    type: array
    description: 'Nested steps will be here. The same structure is used for them.'
    items:
      type: object
