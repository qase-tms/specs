allOf:
  - $ref: 'TestCaseParameterBase.yaml'
  - type: object
    description: Group parameter
    properties:
      type:
        type: string
        enum: [group]
      items:
        type: array
        items:
          $ref: 'Parameter.Single.yaml'
        minItems: 2
    required:
      - items
