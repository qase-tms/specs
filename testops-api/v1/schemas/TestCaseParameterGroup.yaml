allOf:
  - $ref: 'TestCaseParameterBase.yaml'
  - type: object
    description: Group parameter
    properties:
      type:
        type: string
        enum: [group]
      items:
        minItems: 2
