allOf:
  - $ref: 'TestCaseParameterBase.yaml'
  - type: object
    description: Single parameter
    properties:
      type:
        type: string
        enum: [single]
      item:
        $ref: 'Parameter.Single.yaml'
    required:
      - item
