allOf:
  - $ref: 'TestCaseParameterBase.yaml'
  - type: object
    description: Single parameter
    properties:
      type:
        type: string
        enum: [single]
      items:
        minItems: 1
        maxItems: 1
