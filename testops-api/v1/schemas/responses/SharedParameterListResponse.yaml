allOf:
  - $ref: './BaseResponse.yaml'
  - type: object
    properties:
      result:
        type: object
        properties:
          total:
            type: integer
          entities:
            type: array
            items:
              $ref: '../SharedParameter.yaml'
        required:
          - total
          - entities
